import type { CalculatorMode } from '$lib/domain/calculator';

export const calculatorModes: Record<CalculatorMode, { title: string }> = {
  time: {
    title: 'Is it worth the time?',
  },
  money: {
    title: 'Is it worth the time?',
  },
};
